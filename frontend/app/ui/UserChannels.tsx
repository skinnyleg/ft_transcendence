
import type { FC } from 'react';

interface UserChannelsProps {}

const UserChannels: FC<UserChannelsProps> = ({}) => {
		return (
		<div>
			<h1>all the user channels</h1>
		</div>
	);
}
export default UserChannels;
